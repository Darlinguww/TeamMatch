
  # Prototipo - TeamMatch

  This is a code bundle for Prototipo - TeamMatch. The original project is available at https://www.figma.com/design/Ya9PDcQ3InALIqYkgXNStG/Prototipo---TeamMatch.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  